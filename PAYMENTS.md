# نظام الدفع الأوتوماتيكي — عقد الـ API

الباك إند **جاهز وشغّال**. الملف ده هو العقد اللي تطبيق الأندرويد هيتكلم بيه معاه.

---

## الفكرة في سطر

التليفون بيقرا الرسالة ويبعت اللي فيها. **السيرفر هو اللي بيقرر** لو ده يفعّل كورس ولا لأ.
التطبيق نفسه **مش بيفعّل أي حاجة** — ده مقصود، عشان غلطة في التطبيق ما تديش كورس ببلاش.

```
رسالة تحويل توصل التليفون
        ↓
التطبيق يقراها ويستخرج: المبلغ + رقم المحوِّل + الرقم المرجعي
        ↓
POST /api/payments/receive
        ↓
السيرفر يدوّر على طلب معلّق: نفس الرقم + نفس المبلغ
        ↓
   ┌────────────────────┬──────────────────────────┐
 طلب واحد بالظبط        صفر أو أكتر من واحد
   ↓                      ↓
 تفعيل تلقائي فورًا      يستنى قرارك في /admin
```

---

## قاعدة التفعيل التلقائي

الكورس بيتفعّل **بس** لما الشروط دي كلها تتحقق:

| # | الشرط |
|---|---|
| ١ | رقم المحوِّل يطابق رقم مكتوب في طلب معلّق |
| ٢ | المبلغ يطابق مبلغ الطلب بالظبط |
| ٣ | **طلب واحد بالظبط** ينطبق عليه ده |
| ٤ | التحويل ده ما اتسجّلش قبل كده |

أي حالة تانية → بتروح لخانة **"تحويلات محتاجة مراجعة"** في `/admin` وتقرر أنت.

**ليه بالتشدد ده؟** لأن التفعيل الغلط بيدّي كورس ببلاش، والمراجعة اليدوية بتكلّف دقيقة.
الغموض دايمًا بيروحلك أنت، مش للتفعيل التلقائي.

---

## الـ Endpoint

```
POST https://tawwerni.com/api/payments/receive
Authorization: Bearer <PAYMENT_INGEST_TOKEN>
Content-Type: application/json
```

### الطلب

```json
{
  "provider": "vodafone_cash",
  "amount": 200,
  "currency": "EGP",
  "transaction_id": "TX123456",
  "reference_number": "REF123",
  "sender_phone": "01099887766",
  "receiver_phone": "01200176755",
  "transaction_date": "2026-08-25T10:00:00Z",
  "sms_received_at": "2026-08-25T10:00:05Z",
  "raw_sms": "نص الرسالة كامل"
}
```

**المطلوب إجباري:** `provider` (`vodafone_cash` أو `instapay`) · `amount` · `raw_sms`
**الباقي اختياري** بس `sender_phone` من غيره مفيش مطابقة تلقائية أبدًا.

### الردود

| الحالة | HTTP | المعنى | التطبيق يعمل إيه |
|---|---|---|---|
| اتفعّل | 200 `success:true` | الكورس اتفتح للعميل | يعلّم العملية `VERIFIED` |
| مش مطابق | 200 `success:false` | اتسجّل، مستني مراجعة | يعلّمها `SENT` — **متبعتهاش تاني** |
| مكرر | 409 | العملية دي وصلت قبل كده | يعلّمها `SENT` — **متبعتهاش تاني** |
| توكن غلط | 401 | مشكلة إعداد | يوقف ويعرض تنبيه — **متكررش** |
| بيانات ناقصة | 422 | الرسالة مش مفهومة | يعلّمها `REJECTED` — **متكررش** |
| مشكلة سيرفر | 5xx | مشكلة مؤقتة | **يعيد المحاولة** بتباعد متزايد |

> مهم: **400/401/409/422 متتكررش أبدًا** — دي أخطاء نهائية.
> الإعادة بس في 5xx وانقطاع الشبكة.

مثال رد التفعيل:

```json
{
  "success": true,
  "message": "Payment verified",
  "transaction_id": "cmt7s...",
  "order_id": "cmt7s...",
  "user_email": "customer@example.com",
  "course_title": "نمط النجاح - 28 يوم",
  "activated": true
}
```

---

## منع التكرار

بيتعمل على مستويين — التطبيق **والسيرفر**:

1. `transaction_id` أو `reference_number` لو موجود (فريد في قاعدة البيانات)
2. لو مفيش، بصمة `SHA-256` من: المزوّد + المبلغ + الراسل + المستقبل + الوقت + نص الرسالة

يعني حتى لو التطبيق بعت نفس العملية مرتين، السيرفر هيرد 409 ومش هيفعّل تاني.
**ده متختبر فعليًا.**

---

## الجدول في قاعدة البيانات

```
PaymentTransaction
  id, provider, amountEgp, currency
  transactionRef   (فريد لو موجود)
  fingerprint      (فريد — البديل)
  senderPhone, receiverPhone      ← بيتخزنوا آخر 10 أرقام للمطابقة
  transactionAt, smsReceivedAt
  rawSms
  status           unmatched | matched | ignored
  matchedOrderId → Order
  matchNote        سبب عدم المطابقة بالعربي
```

---

## الإعداد على السيرفر

متغيّر بيئة واحد زيادة:

```
PAYMENT_INGEST_TOKEN=<توكن طويل عشوائي>
```

نفس القيمة بتتحط في إعدادات التطبيق. لتوليد واحد:

```bash
node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
```

> التوكن ده اللي بيحمي الـ endpoint. **لو تسرّب، أي حد يقدر يفعّل كورسات.**
> غيّره لو شكيت في أي حاجة.

---

## اللي اتختبر فعليًا

| السيناريو | النتيجة |
|---|---|
| تحويل مطابق تمامًا | ✅ اتفعّل، رجّع إيميل العميل والكورس |
| نفس التحويل تاني | ✅ 409 مكرر — **ما فعّلش تاني** |
| رقم مش معروف | ✅ اتسجّل للمراجعة، ما فعّلش |
| مبلغ مختلف | ✅ اتسجّل للمراجعة، ما فعّلش |
| **طلبين بنفس الرقم والمبلغ** | ✅ **ما فعّلش ولا واحد** — استنى القرار |
| ربط يدوي من `/admin` | ✅ فعّل الطلب المختار بس، والتاني فضل معلّق |
| توكن غلط | ✅ 401 |
| مبلغ صفر / مزوّد غريب / رسالة فاضية | ✅ 422 |

---

## الـ Parser — جاهز ومختبر على رسائل حقيقية

اتكتب من عينات فعلية، مش تخمين. الاختبارات في `scripts/test-parsers.ts`:

```bash
npx tsx scripts/test-parsers.ts
```

| العنصر | فودافون كاش | إنستاباي |
|---|---|---|
| المبلغ | ✅ (`500.00`) | ✅ (`5,000.00` بالفاصلة) |
| رقم العملية | ✅ | ✅ |
| رقم المحوِّل | ✅ | ❌ **مش موجود في الرسالة** |
| اسم المحوِّل | ✅ عربي ولاتيني | ✅ |
| المحفظة المستقبِلة | ✅ | آخر ٤ أرقام بس |
| التاريخ | ✅ `21:50 26-08-19` | ✅ `28-02-2026 · 16:50` |

**بيدعم صيغتين مختلفتين لفودافون:** `من رقم X` و `من X؛` · `تاريخ العملية:` و `بتاريخ`.

**اتأكدنا إنه:** ما بيقراش الرصيد (`رصيدك الحالي: 3909.52`) كمبلغ · وبيرفض التحويلات
الصادرة والإعلانات وأكواد التفعيل.

---

## المطابقة بتختلف حسب المزوّد

### فودافون كاش → بالرقم (أوتوماتيكي)

الرسالة فيها رقم المحوِّل، فبنطابقه مع الرقم اللي العميل كتبه في الطلب.

### إنستاباي → بالاسم (أوتوماتيكي لو الاسم مطابق تمامًا)

رسالة إنستاباي **مفيهاش رقم موبايل خالص** — اسم بس. عشان كده:

- العميل لما يختار إنستاباي، بنطلب منه **اسمه زي ما هو على حسابه في البنك**
- بنطابق الاسمين بعد توحيد شكل الحروف (`أ إ آ` → `ا` · `ى` → `ي` · `ة` → `ه`)
- **كل كلمات الاسم لازم تتطابق** — اسم أول مشترك زي «محمد» مش كفاية أبدًا
- أي تشابه ناقص → مراجعة يدوية

---

## حماية إضافية: المحفظة المستقبِلة

التحويل اللي بيوصل على محفظة **مش من أرقامنا المعلنة** بيترفض تلقائيًا ويروح
للمراجعة. يعني تحويل شخصي على محفظتك الخاصة عمره ما هيفعّل كورس بالغلط.

الأرقام المعتمدة بتتقرا من `src/content/brand.ts` — لو غيّرتها، الحماية بتتحدّث معاها.

---

## ⚠️ مهم جدًا: شرايح التليفون المخصص

رسالة فودافون كاش بتوصل على **الخط اللي استلم الفلوس**.

الأرقام المعلنة للدفع دلوقتي: **01200176755** و **01067558133**

يعني التليفون المخصص **لازم يكون فيه الشريحتين دول** (تليفون بشريحتين، أو
تليفونين والتطبيق على الاتنين). لو شريحة واحدة بس موجودة، التحويلات اللي على
الرقم التاني **مش هيشوفها التطبيق خالص** وهتفضل مستنية مراجعة يدوية.

**أبسط حل لو تحب:** تعلن رقم واحد بس للدفع = شريحة واحدة = مفيش تحويل ممكن يضيع.

# الرفع من الصفر — GitHub ثم Hostinger

كل حاجة اتمسحت، فبنبدأ من أول وجديد. الدليل ده مكتوب على أساس إن خبرتك زيرو:
كل أمر تنسخه زي ما هو، وكل ضغطة زرار مكتوب مكانها بالظبط.

**الفكرة اللي بتخلي المرة دي أسهل من اللي فاتت:**
كل شغل قاعدة البيانات بيتعمل **على جهازك**، وبعدين بترفع ملف واحد جاهز.
يعني مش محتاج تكتب أي أمر على السيرفر خالص.

```
جهازك                     GitHub                  Hostinger
─────                     ──────                  ─────────
الكود ────── git push ──────→ الريبو ─── Deploy ───→ public_html
dev.db ─────────── File Manager (رفع مباشر) ───────→ public_html
```

---

## قبل ما تبدأ — حالة الكود دلوقتي

اتفحص فعليًا، والنتيجة:

| الفحص | النتيجة |
|---|---|
| `tsc --noEmit` | ✅ صفر أخطاء |
| `npm run build` | ✅ اتبنى كامل |
| اختبارات قارئ رسايل الدفع | ✅ كلها نجحت |
| شغل غير محفوظ في git | ✅ مفيش — كله متسجّل |
| أسرار متسرّبة في الكود | ✅ مفيش · `.env` و`dev.db` مستثنيين |
| المحتوى | ✅ ٦ مسارات · ١٥٧ درس |

الريبو **آمن إنه يبقى Public** — وده اللي هيحل مشكلة المصادقة اللي وقّفتنا قبل كده.

---

# المرحلة ١ — جهازك (٥ دقايق)

افتح **cmd** واكتب:

```bash
cd /d H:\ClaudeAI\tawwerni
```

## ١.١ حضّر قاعدة البيانات

الأوامر دي بتخلّي `dev.db` جاهز تمامًا قبل ما يسافر:

```bash
npx prisma db push
```

```bash
npm run db:seed
```

## ١.٢ اعمل باسورد الأدمن

**الإيميل:** `tawwarni@gmail.com`

**الباسورد:** مخزّن مشفّر باتجاه واحد — مستحيل يترجع، حتى مني. فهتعمل واحد جديد.

اختار باسورد قوي وحطه مكان `الباسورد-الجديد`:

```bash
npx tsx scripts/create-admin.ts "tawwarni@gmail.com" "الباسورد-الجديد" "Hifzy"
```

> اكتبه في مكان آمن. ده مفتاح لوحة التحكم كلها.

## ١.٣ اطبع متغيّرات البيئة

```bash
npx tsx scripts/print-env.ts
```

هيطبع بلوك جاهز. **سيب الشاشة مفتوحة** — هتنسخ منها في المرحلة ٣.

---

# المرحلة ٢ — GitHub (٣ دقايق)

## ٢.١ اعمل ريبو جديد

روح [github.com/new](https://github.com/new):

| الحقل | اللي تحطه |
|---|---|
| Repository name | `Tawwerni` |
| Visibility | **Public** ← مهم |
| README / .gitignore / license | **سيبهم فاضيين** |

اضغط **Create repository**.

> **ليه Public؟** لأن ده اللي بيخلي Hostinger يسحب الكود من غير كلمة سر —
> ودي بالظبط المشكلة اللي وقّفتنا. الفحص فوق أكّد إن مفيش أي سر في الكود:
> الباسوردات والتوكنات كلها في متغيّرات البيئة على السيرفر، مش في الملفات.

## ٢.٢ ارفع الكود

بدّل `اسم-حسابك` باسمك على GitHub:

```bash
git remote set-url origin https://github.com/اسم-حسابك/Tawwerni.git
```

```bash
git push -u origin main
```

هيطلب تسجيل دخول. **الباسورد العادي مش هينفع** — لازم Personal Access Token:

1. [github.com/settings/tokens/new](https://github.com/settings/tokens/new)
2. Note: `hostinger` · Expiration: `No expiration` · علّم على **`repo`**
3. **Generate token** → انسخه فورًا (مش هيظهر تاني)
4. استخدمه مكان الباسورد

**اتأكد إنه وصل:** افتح الريبو في المتصفح — لازم تشوف `src` و`prisma`
و`package.json`. لو شايفهم، المرحلة خلصت ✅

---

# المرحلة ٣ — Hostinger (١٥ دقيقة)

## ٣.١ نظّف المجلد

hPanel → **File Manager** → `domains/tawwerni.com/public_html`

امسح **كل** اللي جوّه. المجلد لازم يبقى فاضي تمامًا.

## ٣.٢ اربط GitHub

hPanel → **Websites** → `tawwerni.com` → **Advanced** → **GIT**

| الحقل | اللي تحطه |
|---|---|
| Repository URL | `https://github.com/اسم-حسابك/Tawwerni.git` |
| Branch | `main` |
| Directory | **سيبه فاضي** (يعني `public_html` نفسه) |

اضغط **Create**، وبعدها **Deploy**.

روح File Manager تاني — المفروض تلاقي `src` و`prisma` و`package.json`.

> **من هنا ورايح:** أي تحديث = `git push` من جهازك + ضغطة **Deploy** هنا.

## ٣.٣ ارفع قاعدة البيانات

`dev.db` **مش** في GitHub بقصد — فيه بيانات العملاء، ومكانه مش ريبو عام.

من **File Manager** وانت جوّه `public_html`:
**Upload** → اختار `H:\ClaudeAI\tawwerni\dev.db`

> الملف ده فيه الحسابات والطلبات والتقدّم والإحالات. الكود كله ممكن يترفع
> تاني في دقيقتين — الملف ده لأ. **خلّي نسخة منه على جهازك دايمًا.**

## ٣.٤ ظبّط تطبيق الـ Node.js

hPanel → **Websites** → `tawwerni.com` → **Node.js**

| الإعداد | القيمة |
|---|---|
| Node.js version | **20** أو **22** |
| Application root | `public_html` |
| Application startup file | `server.js` |
| Application mode | `Production` |

**Save**.

## ٣.٥ حط متغيّرات البيئة

نفس الصفحة، جزء **Environment Variables**. ارجع للبلوك اللي طبعته في ١.٣:

| المتغيّر | القيمة | ليه |
|---|---|---|
| `DATABASE_URL` | `file:./dev.db` | مكان قاعدة البيانات |
| `JWT_SECRET` | من البلوك | **متغيّرهوش أبدًا** — لو اتغيّر كل الناس هتتطرد |
| `PUBLIC_ORIGIN` | `https://tawwerni.com` | لينكات الإيميل وبطاقة الواتساب |
| `PAYMENT_INGEST_TOKEN` | من البلوك | **لازم يطابق تطبيق الأندرويد بالحرف** |
| `NODE_ENV` | `production` | |

**Save**.

> `ADMIN_PASSWORD` القديم **متحطهوش** — اتشال من الكود خلاص. الدخول بقى
> إيميل + باسورد من قاعدة البيانات.

## ٣.٦ ثبّت وابنِ

في صفحة الـ Node.js، بالترتيب ده:

1. **Run npm install** ← استنى تخلص (٢-٣ دقايق)
2. **Run script** → اختار **`build`** ← استنى (٣-٥ دقايق)
3. **Restart Application**
4. **امسح الكاش** — Dashboard → **Performance** / **Cache Manager** → **Clear Cache**

> **الخطوة الرابعة مش اختيارية.** Hostinger بيخزّن نسخة من كل صفحة. من غير
> مسح، هتفتح الموقع وتشوف النسخة **القديمة** وتفتكر إن الرفع فشل — والأسوأ:
> الصفحة القديمة المخزّنة بتطلب ملفات جافاسكربت من البناء القديم، واللي
> اتمسحت وقت البناء الجديد، فبتطلع شاشة `This page couldn't load`.
>
> علامة مميزة للمشكلة دي: الصفحات **الجديدة** تشتغل والقديمة لأ — لأن
> الجديدة مالهاش نسخة مخزّنة أصلًا.

> **متر فعش مجلد `.next` من جهازك أبدًا.** ده اللي كسر الموقع المرة اللي
> فاتت: ملف واحد ناقص = الصفحة بتطلب ملف مش موجود = تصميم مختفي وشاشة سودا
> في `/login`. خلّي البناء يحصل على السيرفر.

## ٣.٧ افتح الموقع

`tawwerni.com` — المفروض تشوف تصميم كامل: خلفية غامقة، السعر ٢٩٩ جنيه، ٦ مسارات.

---

# المرحلة ٤ — الإيميلات (١٠ دقايق)

من غيرها: مفيش رسايل ترحيب، ومفيش لينكات "نسيت كلمة السر".

## ٤.١ اعمل صندوق بريد

hPanel → **Emails** → **Email Accounts** → **Create**

- الاسم: `noreply` · الدومين: `tawwerni.com`
- الباسورد: قوي — **احفظه**

## ٤.٢ ضيف المتغيّرات

**Node.js** → **Environment Variables**:

| المتغيّر | القيمة |
|---|---|
| `SMTP_HOST` | `smtp.hostinger.com` |
| `SMTP_PORT` | `465` |
| `SMTP_USER` | `noreply@tawwerni.com` |
| `SMTP_PASS` | باسورد الصندوق |
| `EMAIL_FROM` | `noreply@tawwerni.com` |

**Save** → **Restart Application**

## ٤.٣ اتأكد إنها بتوصل

افتح `tawwerni.com/admin` وادخل بإيميلك وباسوردك.

فوق الصفحة **حالة الإيميل** وزرار **ابعت تجربة**. ابعت لنفسك وشوف.

| اللي بيظهر | المعنى |
|---|---|
| "مش متظبط" | المتغيّرات ناقصة — أو نسيت Restart |
| "فشل الإرسال" | باسورد أو Host غلط |
| وصل Spam | طبيعي في أول أيام · هيتحسّن |

**الإيميلات الأربعة:** أهلًا بيك · كود الدخول · إيصال الطلب · تفعيل الاشتراك.

---

# المرحلة ٥ — الدفع (١٠ دقايق)

## ٥.١ الأرقام المعلنة

مكتوبة في `src/content/brand.ts`:

| الطريقة | الأرقام |
|---|---|
| فودافون كاش | `01200176755` · `01067558133` |
| إنستاباي | `hhifzy@instapay` · `01067558133` |

> أي تحويل يوصل على محفظة **مش** من دول بيترفض تلقائيًا ويروح للمراجعة.
> يعني تحويل شخصي على محفظتك الخاصة عمره ما هيفعّل كورس بالغلط.

## ٥.٢ ⚠️ الشرايح — أهم نقطة في الجزء ده

رسالة فودافون كاش بتوصل على **الخط اللي استلم الفلوس**.

انت معلن **رقمين**. يعني التليفون المخصص **لازم يكون فيه الشريحتين**
(تليفون بشريحتين، أو تليفونين والتطبيق على الاتنين).

**لو شريحة واحدة بس، تحويلات الرقم التاني التطبيق مش هيشوفها خالص** وهتفضل
مستنية مراجعة يدوية منك.

**أبسط حل:** تعلن رقم واحد بس = شريحة واحدة = مفيش تحويل ممكن يضيع.
لو عايز كده، غيّر `src/content/brand.ts` وارفع تاني.

## ٥.٣ ظبّط التطبيق

| الإعداد | القيمة |
|---|---|
| Endpoint | `https://tawwerni.com/api/payments/receive` |
| Token | نفس `PAYMENT_INGEST_TOKEN` **بالحرف** |

## ٥.٤ جرّب بتحويل حقيقي صغير

1. من موبايل تاني، اعمل طلب على الموقع
2. حوّل فعليًا على الرقم
3. الرسالة توصل التليفون المخصص → التطبيق يبعتها → **الكورس يتفعّل تلقائيًا**

لو ما اتفعّلش، افتح `/admin` → **تحويلات محتاجة مراجعة** — هتلاقي التحويل
مسجّل ومكتوب جنبه سبب عدم المطابقة بالعربي.

> التفعيل التلقائي بيحصل **بس** لو: الرقم مطابق + المبلغ مطابق + **طلب واحد
> بالظبط** ينطبق عليه. أي غموض بيروحلك أنت — عشان غلطة ما تديش كورس ببلاش.

**إنستاباي بيتطابق بالاسم** — رسالته مفيهاش رقم موبايل خالص. عشان كده
بنطلب من العميل اسمه زي ما هو على حسابه في البنك، وكل كلمات الاسم لازم
تتطابق. اسم أول مشترك زي «محمد» مش كفاية.

---

# المرحلة ٦ — الفحص النهائي

| # | افتح | المفروض تشوف |
|---|---|---|
| ١ | `tawwerni.com` | تصميم كامل · السعر ٢٩٩ · ٦ مسارات |
| ٢ | `/login` | فورم دخول — **مش شاشة سودا** |
| ٣ | `/forgot-password` | صفحة طلب لينك |
| ٤ | `/admin` | دخول بإيميلك، وبعدها لوحة الطلبات |
| ٥ | `/admin` | **حالة الإيميل** = متظبط ✅ |
| ٦ | `/app` | الهيدر ثابت فوق · ٦ مسارات |
| ٧ | `/sitemap.xml` | قايمة صفحات |

**اختبار بطاقة الواتساب:** ابعت `https://tawwerni.com` لنفسك على واتساب.
لازم تظهر بطاقة خضرا فيها اللوجو والسعر — مش لينك أزرق فاضي.

---

# لو حصلت مشكلة

| المشكلة | الحل |
|---|---|
| صفحات جديدة شغّالة والقديمة لأ | **كاش** — Clear Cache، وبعدها `Ctrl+Shift+R` في المتصفح |
| `This page couldn't load` | نفس السبب — صفحة قديمة مخزّنة بتطلب ملفات اتمسحت |
| الموقع من غير تصميم | البناء ما خلصش — **Run script → build** تاني + Restart + Clear Cache |
| شاشة سودا في `/login` | نفس السبب فوق |
| `500` في كل الصفحات | متغيّر ناقص — راجع `JWT_SECRET` و`DATABASE_URL` |
| مشكلة قاعدة بيانات | `dev.db` ما اترفعش — ارفعه من File Manager |
| نسيت باسورد الأدمن | اعمل واحد جديد محليًا (خطوة ١.٢) وارفع `dev.db` تاني |

---

# التحديثات بعد كده

```bash
git add -A && git commit -m "وصف التغيير" && git push
```

وبعدين في hPanel، بالترتيب ده:

**GIT → Deploy** ← **Run npm install** ← **Run script → build** ← **Restart** ← **Clear Cache**

> متبطّلش خطوة **Clear Cache**. من غيرها هتفتح الموقع، تشوف القديم، وتفتكر
> إن التحديث ما وصلش.

> لو التحديث فيه تغيير في قاعدة البيانات: خد نسخة من `dev.db` اللي على
> السيرفر الأول (فيه عملاء جداد)، شغّل عليها `npx prisma db push` على جهازك،
> وارفعها تاني.

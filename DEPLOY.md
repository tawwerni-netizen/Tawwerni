# رفع "طوّرني" على Hostinger

> ## 🔑 تحديث الدخول — اقرا ده
>
> الدخول بقى **إيميل + باسورد** بدل كود الـ OTP. حسابك جاهز:
>
> ```
> Tawwarni@gmail.com
> ```
>
> الباسورد اللي بعتهولي — **مخزّن مشفّر باتجاه واحد**، يعني مستحيل يترجع
> حتى مني. لو نسيته، شغّل الأمر ده على السيرفر بباسورد جديد:
>
> ```bash
> npx tsx scripts/create-admin.ts "Tawwarni@gmail.com" "الباسورد-الجديد" "Hifzy"
> ```
>
> **غيّر الباسورد ده بعد أول دخول** — انت كتبته في الشات، فاعتبره مكشوف.


> ## 🔴 رفع تحديث على موقع شغّال؟ اقرا ده الأول
>
> أي تحديث فيه تغيير في قاعدة البيانات **لازم** تشغّل الأمر ده بعد الرفع،
> من **Terminal** في hPanel وأنت جوّه مجلد التطبيق:
>
> ```bash
> npx prisma db push
> ```
>
> بعده اعمل **Restart Application**.
>
> الأمر ده **بيضيف الأعمدة والجداول الناقصة بس** — عمره ما بيمسح بيانات.
> لو نسيته، هتلاقي `/admin` بيدّي خطأ وصفحة الدفع بتقف — لأن الكود بيدوّر
> على جداول مش موجودة. (دي بالظبط المشكلة اللي حصلت في أول رفعة.)
>
> **متشغّلش `npm run db:setup` تاني بعد ما يبقى عندك عملاء** — ده بيعيد
> زرع المحتوى. للتحديثات استخدم `npx prisma db push` وبعدها `npm run db:seed`
> لو غيّرت محتوى الدروس.


دليل خطوة بخطوة لرفع الموقع على `tawwerni.com`.

> **مهم:** الموقع ده Next.js — محتاج **Node.js شغّال على السيرفر**، مش استضافة ملفات ساكنة.
> ده متاح في باقات **Business / Cloud** من خلال **Node.js Apps** في hPanel.

---

## قبل ما تبدأ — جهّز سرّين

القيم دي بتتحط في **إعدادات البيئة على Hostinger**، مش في الكود:

| المتغيّر | إيه هو | القيمة |
|---|---|---|
| `DATABASE_URL` | مكان قاعدة البيانات | `file:./dev.db` |
| `JWT_SECRET` | مفتاح تشفير جلسات الدخول | نصّ عشوائي طويل (الأمر تحت) |

لتوليد `JWT_SECRET`، شغّل على جهازك:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
```

> **مفيش `ADMIN_PASSWORD` تاني.** لوحة التحكم بقت بتفتح بحساب المالك نفسه
> (إيميل + باسورد) — الباسورد مخزّن مشفّر في الداتابيز مش في إعدادات
> الاستضافة. الحساب بيتعمل بأمر واحد بعد الرفع، شوف قسم **حساب المالك** تحت.

> `ANTHROPIC_API_KEY` **مطلوب عشان مساعد "فهيم" يشتغل.** من غيره، الشات
> بيرجّع رسالة واحدة بس بتقول إن المفتاح ناقص — مفيش بنك أسئلة جاهز بديل.
> خده من [console.anthropic.com](https://console.anthropic.com).

---

# الطريقة الأولى: الرفع من Git (الأسهل — ينصح بيها)

Hostinger بيقرا المشروع لوحده ويحدد إعدادات البناء.

### ١. ارفع المشروع على GitHub

من داخل مجلد `tawwerni`:

```bash
git init && git add -A && git commit -m "طوّرني - النسخة الأولى"
```

بعدين اعمل repository جديد **private** على GitHub واربطه:

```bash
git remote add origin <رابط الريبو بتاعك> && git push -u origin main
```

> ملف `.env.local` و `dev.db` **مش هيترفعوا** — متسجّلين في `.gitignore` عشان أسرارك تفضل عندك.

### ٢. اعمل الموقع في hPanel

**Websites** → **Add Website** → **Node.js Apps** → **Import Git Repository**

اربط حساب GitHub واختار الريبو. Hostinger هيتعرّف على Next.js تلقائيًا.

### ٣. حط متغيّرات البيئة

قبل ما تدوس Deploy، ضيف التلات متغيّرات من قسم **Environment variables**.

### ٤. Deploy

دوس **Deploy** واستنى البناء يخلص.

### ٥. ازرع المحتوى (مرة واحدة بس)

بعد أول نشر ناجح، افتح **Terminal / SSH** في مجلد التطبيق وشغّل:

```bash
npm run db:setup
```

ده بيعمل قاعدة البيانات ويزرع فيها المسارات الخمسة (140 درس، 560 سؤال).

> ⚠️ **شغّله مرة واحدة بس.** لو كرّرته بعد ما يبقى عندك طلاب، هيمسح تقدّمهم.
> للتحديثات بعدين استخدم `npm run db:seed` (بيحدّث المحتوى من غير ما يمسح حاجة).

---

# الطريقة التانية: الرفع اليدوي بالملفات

لو مش عايز تستخدم Git.

### ١. ابنِ المشروع على جهازك

```bash
npm run build
```

### ٢. ارفع الملفات

من hPanel → **File Manager** (أو FTP)، ارفع محتويات مجلد `tawwerni` جوه مجلد الدومين.

**ارفع** `.next` و `server.js` و `package.json` و `src` و `prisma` و `public`.

**متر فعش**: `node_modules/` و `.env.local` و `dev.db`.

### ٣. اعمل Node.js App

hPanel → **Advanced** → **Setup Node.js App** → **Create Application**:

| الحقل | القيمة |
|---|---|
| Node.js version | **20** أو أحدث |
| Application mode | **Production** |
| Application root | مسار مجلد الدومين |
| Application URL | `tawwerni.com` |
| **Application startup file** | **`server.js`** |

> ملف `server.js` موجود في المشروع ومعمول مخصوص للاستضافة دي — بيشغّل Next.js
> على البورت اللي السيرفر بيديهوله. **متغيّرهوش.**

### ٤. حط متغيّرات البيئة

في نفس الشاشة، ضيف التلات متغيّرات من الجدول اللي فوق.

### ٥. ثبّت وازرع

دوس **Run NPM Install**، بعدين من **Terminal**:

```bash
npm run db:setup
```

### ٦. شغّل

دوس **Restart Application**.

---

## حساب المالك (لوحة التحكم)

لوحة `/admin` بتفتح بحساب عادي على الموقع متعلّم عليه إنه أدمن. اعمله من
**Terminal** على الاستضافة مرة واحدة:

```bash
npx tsx scripts/create-admin.ts "الإيميل-بتاعك" "الباسورد-اللي-تختاره" "اسمك"
```

الباسورد بيتخزن **مشفّر في اتجاه واحد** (scrypt). يعني حتى أنا مش شايفه، ولو
حد سرق ملف الداتابيز مش هيقدر يطلّعه منه. لو نسيته، شغّل نفس الأمر بباسورد
جديد — بيحدّث الحساب القديم.

نفس الأمر بيرقّي أي حساب موجود لأدمن. ومحدش غير أصحاب `isAdmin` بيقدر يفتح
اللوحة، حتى لو باسورده صح.

---

## بعد الرفع — أول 3 حاجات تعملها

1. **افتح `tawwerni.com`** — لازم الصفحة الرئيسية تفتح **بالتصميم كامل**. لو
   ظهرت نص أسود على أبيض من غير تنسيق، ده معناه إن `.next` اترفع ناقص —
   شوف قسم المشاكل تحت.
2. **افتح `tawwerni.com/admin`** وادخل بحساب المالك اللي عملته فوق.
3. **اعمل طلب تجريبي** من `tawwerni.com/quiz/checkout`، شوفه ظهر في `/admin`، وجرّب "منح الوصول ✓".

---

## دورة العمل اليومية بتاعتك

```
العميل يدفع (فودافون كاش / إنستاباي)
        ↓
يبعتلك صورة التحويل على واتساب 01069999557
أو Tawwerni@gmail.com
        ↓
تفتح tawwerni.com/admin
        ↓
تطابق الإيميل + المسار مع الرسالة
        ↓
تدوس "منح الوصول ✓"
        ↓
العميل يدخل بإيميله ويلاقي المسار مفتوح
```

**اليوم الأول من كل مسار مفتوح مجانًا للكل** — ده اللي بيخلّي الناس تجرّب قبل ما تدفع.
باقي الـ 27 يوم بيتفتحوا بس بعد ما تدوس "منح الوصول".

---

## تحديث المحتوى بعدين

لو عدّلت أي درس في `src/content/`:

**مع Git:** اعمل push والنشر هيحصل تلقائي، بعدين شغّل `npm run db:seed`.

**يدوي:** `npm run build` على جهازك → ارفع `.next` و `src` → `npm run db:seed` → **Restart**.

> ⚠️ **متر فعش مجلد `.next` جاهز من جهازك.** الطريقة دي هي اللي كسرت الموقع
> قبل كده: لو ملف واحد من `.next/static/chunks` ما اترفعش (اتصال قطع، أو
> الـ File Manager تخطّاه)، الصفحة بتطلب ملف مش موجود وبتقع — تصميم مختفي في
> الرئيسية، وشاشة سودا في `/login`. ارفع الكود بس وشغّل `npm run build`
> **على السيرفر**، عشان الـ HTML والملفات يطلعوا من نفس البناء.

`db:seed` بيحدّث الدروس **من غير** ما يمسح تقدّم الطلاب ولا طلباتهم.

---

## نسخة احتياطية (مهم جدًا)

كل بيانات الموقع — المستخدمين، الطلبات، التقدّم — في ملف واحد: **`dev.db`**

**نزّله نسخة كل أسبوع** من File Manager. لو ضاع، تضيع البيانات كلها.

---

## مشاكل شائعة

| المشكلة | الحل |
|---|---|
| صفحة بيضا أو خطأ 500 | شوف الـ logs من Node.js App، وتأكد إن المتغيّرات موجودة |
| الموقع مش بيفتح خالص | تأكد إن **startup file** = `server.js` بالظبط |
| **الموقع من غير تصميم** (نص أسود على أبيض) | `.next` ناقص ملفات. شغّل `npm run build` على السيرفر + Restart |
| **`/login` بيدّي شاشة سودا** | نفس السبب — ملف JS مفقود من `.next/static/chunks`. نفس الحل |
| `/admin` بيقول الإيميل أو الباسورد غلط | الحساب مش أدمن (أو مش موجود) — شغّل `scripts/create-admin.ts` |
| الدروس فاضية | ما شغّلتش `npm run db:setup` |
| عدّلت محتوى وما ظهرش | لازم build جديد + Restart |
| الخط العربي شكله مختلف | السيرفر ما وصلش لـ Google Fonts وقت البناء — الموقع شغّال عادي بخط بديل |

# الرفع النظيف — مسح النسخة القديمة ورفع الجديدة

الدليل ده للحالة اللي إنت فيها: على السيرفر نسخة قديمة أو ناقصة أو اترفعت
بطريقة غلط، وعايز تبدأ من أول وجديد **من غير ما تفقد بيانات العملاء**.

---

## ⚠️ اقرا الفقرة دي قبل أي حاجة

في مجلد التطبيق ملف واحد لا يُعوّض:

```
dev.db
```

فيه **كل حاجة**: الحسابات، الباسوردات، الطلبات، التقدّم، الإحالات، الأرصدة.
الكود كله ممكن يترفع تاني في دقيقتين — الملف ده لأ.

**نزّله على جهازك قبل ما تمسح أي حاجة**، من File Manager في hPanel.
دي مش خطوة اختيارية.

> `dev.db` مستثنى من Git بقصد، فأي `git pull` **مش بيلمسه**. الخطر الوحيد
> إنك تمسح المجلد وهو جوّه.

---

## الطريقة المضمونة — Git

### ١. نسخة احتياطية

من **File Manager** → افتح مجلد التطبيق → نزّل `dev.db` على جهازك.
وبعدين من **Terminal**:

```bash
cp dev.db ~/dev.db.backup
```

### ٢. امسح الكود القديم وهات الجديد

```bash
git fetch origin && git reset --hard origin/main && git clean -fd -e dev.db -e .env
```

- `fetch` بيجيب آخر نسخة
- `reset --hard` بيرمي أي تعديل محلي ويطابق الـrepo بالظبط
- `clean -fd` بيمسح أي ملف زيادة **ما عدا** `dev.db` و`.env`

> `-e dev.db -e .env` دي أهم حتة في السطر. من غيرها `git clean` هيمسح قاعدة
> البيانات وإعداداتك.

### ٣. امسح البناء القديم وثبّت من جديد

```bash
rm -rf .next node_modules && npm ci
```

`rm -rf .next` هو اللي بيحل مشكلة الملفات الناقصة اللي كانت مكسّرة الموقع.

### ٤. حدّث القاعدة وابنِ

```bash
npx prisma db push && npm run build
```

`db push` بيضيف الأعمدة والجداول الجديدة **من غير ما يمسح أي بيانات**.
في النسخة دي جدول جديد (`PasswordReset`) — من غيره صفحة "نسيت كلمة السر" هتقع.

### ٥. حدّث الكتالوج

```bash
npm run db:seed
```

بيحدّث الدروس والترتيب في مكانها. **مبيمسحش** تقدّم الطلاب ولا طلباتهم.

> الاستثناء الوحيد: لو درس اتشال خالص من المحتوى، تقدّمه بيتشال معاه — مفيش
> حاجة فاضلة كان فيها تقدّم أصلًا. بيتكتب في الشاشة لما يحصل.

### ٦. حساب المالك

```bash
npx tsx scripts/create-admin.ts "Tawwarni@gmail.com" "باسورد-جديد-قوي" "Hifzy"
```

### ٧. Restart Application

من صفحة الـNode.js App في hPanel.

---

## الطريقة اليدوية (لو مفيش Git)

1. **نزّل `dev.db`** — زي ما فوق.
2. من File Manager، امسح **كل حاجة ما عدا**: `dev.db` · `.env` · `node_modules`
3. ارفع ملفات المشروع الجديدة (**من غير** `.next` و`node_modules`).
4. من Terminal:
   ```bash
   rm -rf .next && npm ci && npx prisma db push && npm run build && npm run db:seed
   ```
5. **Restart Application**

> **متر فعش مجلد `.next` من جهازك.** ده اللي كسر الموقع قبل كده: لو ملف واحد
> منه ما اترفعش، الصفحة بتطلب ملف مش موجود — تصميم مختفي، وشاشة سودا في
> `/login`. خلّي الـbuild يحصل على السيرفر.

---

## متغيّرات البيئة

| المتغيّر | القيمة | ليه |
|---|---|---|
| `DATABASE_URL` | `file:./dev.db` | مكان قاعدة البيانات |
| `JWT_SECRET` | نصّ عشوائي طويل | **متغيّرهوش** — لو غيّرته كل الناس هتتطرد من حساباتها |
| `PUBLIC_ORIGIN` | `https://tawwerni.com` | لينك "نسيت كلمة السر" وبطاقة المشاركة بيتبنوا منه |
| `PAYMENT_INGEST_TOKEN` | نفس اللي في تطبيق الأندرويد | استقبال رسايل الدفع |

**للإيميلات** — من غيرها مفيش رسايل ترحيب ولا لينكات تغيير باسورد:

| المتغيّر | القيمة |
|---|---|
| `SMTP_HOST` | `smtp.hostinger.com` |
| `SMTP_PORT` | `465` |
| `SMTP_USER` | `noreply@tawwerni.com` |
| `SMTP_PASS` | الباسورد |
| `EMAIL_FROM` | `noreply@tawwerni.com` |

> `PUBLIC_ORIGIN` مهم بالذات دلوقتي: بطاقة المعاينة اللي بتظهر لما حد يبعت
> لينك الموقع على واتساب بتتبني منه. لو ناقص، الصورة ممكن ما تظهرش.

---

## بعد الرفع — الفحص

| # | افتح | المفروض تشوف |
|---|---|---|
| ١ | `tawwerni.com` | الصفحة **بتصميم كامل**، السعر ظاهر، والـ٦ مسارات |
| ٢ | `tawwerni.com/login` | فورم دخول — مش شاشة سودا |
| ٣ | `/forgot-password` | صفحة طلب لينك تغيير الباسورد |
| ٤ | `/admin` | فورم إيميل + باسورد، وبعدها لوحة الطلبات |
| ٥ | `/admin` (الجانب) | **حالة الإيميل** — لو "مش متظبط" حط متغيّرات SMTP |
| ٦ | `/app` | الهيدر ثابت فوق، و**٦ مسارات** في الشبكة |
| ٧ | `/sitemap.xml` | قايمة صفحات — يعني جوجل هيلاقي الموقع |

**اختبار بطاقة المشاركة:** ابعت `https://tawwerni.com` لنفسك على واتساب.
المفروض تظهر بطاقة خضرا فيها اللوجو والسعر — مش لينك أزرق فاضي.

**لو الصفحة طلعت من غير تصميم:** الـbuild ما خلصش أو `.next` ناقص —
`rm -rf .next && npm run build` تاني + Restart.

---

## بعد ما يشتغل — خطوتين للانتشار

1. **Google Search Console** — [search.google.com/search-console](https://search.google.com/search-console)
   ضيف `tawwerni.com`، وابعتلهم `https://tawwerni.com/sitemap.xml`.
   ده اللي بيخلي الموقع يبان في جوجل.

2. **جرّب المشاركة بنفسك** — افتح `/app`، دوس "واتساب" في كارت المشاركة،
   وابعت اللينك لنفسك. اللينك ده بيحمل كود الإحالة بتاعك، فأي حد يشترك منه
   بيتحسبلك.

---

## لو حصلت كارثة

```bash
cp ~/dev.db.backup dev.db
```

بعده Restart. البيانات رجعت.

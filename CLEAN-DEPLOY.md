# الرفع النظيف — مسح النسخة القديمة ورفع الجديدة

الدليل ده للحالة اللي إنت فيها بالظبط: على السيرفر نسخة قديمة أو ناقصة أو
اترفعت بطريقة غلط، وعايز تبدأ من أول وجديد **من غير ما تفقد بيانات العملاء**.

---

## ⚠️ اقرا الفقرة دي قبل أي حاجة

في مجلد التطبيق فيه **ملف واحد لا يُعوّض**:

```
dev.db
```

الملف ده فيه **كل حاجة**: الحسابات، الباسوردات، الطلبات، التقدّم، الإحالات،
الأرصدة. الكود كله ممكن يترفع تاني في دقيقتين — الملف ده لأ.

**نزّله على جهازك قبل ما تمسح أي حاجة**، من File Manager في hPanel.
دي مش خطوة اختيارية.

> `dev.db` مش موجود في Git (مستثنى بقصد)، فأي `git pull` أو رفع كود
> **مش بيلمسه**. الخطر الوحيد هو إنك تمسح المجلد وهو جوّه.

---

## الطريقة المضمونة — Git

لو الموقع مربوط بالـ GitHub repo، دي أنضف طريقة وأقلها خطأ.

### ١. خد نسخة احتياطية

من **File Manager** → افتح مجلد التطبيق → نزّل `dev.db` على جهازك.

### ٢. من Terminal في hPanel

```bash
cp dev.db ~/dev.db.backup
```

نسخة تانية برّه المجلد، تحسّبًا.

### ٣. امسح الكود القديم وهات الجديد

```bash
git fetch origin && git reset --hard origin/main && git clean -fd -e dev.db -e .env
```

الأمر ده بيعمل تلات حاجات:
- `fetch` بيجيب آخر نسخة
- `reset --hard` بيرمي أي تعديل محلي ويطابق الـ repo بالظبط
- `clean -fd` بيمسح أي ملف زيادة **ما عدا** `dev.db` و`.env`

> `-e dev.db -e .env` دي أهم حتة في السطر. من غيرها `git clean` هيمسح
> قاعدة البيانات وإعداداتك.

### ٤. امسح البناء القديم وثبّت من جديد

```bash
rm -rf .next node_modules && npm ci
```

`rm -rf .next` هو اللي بيحل مشكلة الملفات الناقصة اللي كانت مكسّرة الموقع —
بيضمن إن مفيش أي بقايا من بناء قديم.

### ٥. حدّث القاعدة وابنِ

```bash
npx prisma db push && npm run build
```

`db push` بيضيف الأعمدة والجداول الجديدة **من غير ما يمسح أي بيانات**.
في النسخة دي فيه جدول جديد (`PasswordReset`) — من غير الأمر ده صفحة
"نسيت كلمة السر" هتقع.

### ٦. حدّث الكتالوج

```bash
npm run db:seed
```

بيحدّث الدروس والترتيب. **مبيمسحش** تقدّم الطلاب ولا طلباتهم.

### ٧. حساب المالك

```bash
npx tsx scripts/create-admin.ts "Tawwarni@gmail.com" "باسورد-جديد-قوي" "Hifzy"
```

### ٨. Restart Application

من صفحة الـ Node.js App في hPanel.

---

## الطريقة اليدوية (لو مفيش Git)

نفس المنطق، بس بالرفع اليدوي:

1. **نزّل `dev.db`** — زي ما فوق.
2. من File Manager، امسح **كل حاجة ما عدا**: `dev.db` · `.env` · `node_modules`
3. ارفع ملفات المشروع الجديدة (من غير `.next` ومن غير `node_modules`).
4. من Terminal: `rm -rf .next && npm ci && npx prisma db push && npm run build && npm run db:seed`
5. **Restart Application**

> **متر فعش مجلد `.next` من جهازك.** ده اللي كسر الموقع قبل كده: لو ملف
> واحد منه ما اترفعش، الصفحة بتطلب ملف مش موجود — تصميم مختفي، وشاشة
> سودا في `/login`. خلّي الـ build يحصل على السيرفر.

---

## متغيّرات البيئة

في إعدادات الـ Node.js App لازم يكون موجود:

| المتغيّر | القيمة |
|---|---|
| `DATABASE_URL` | `file:./dev.db` |
| `JWT_SECRET` | نصّ عشوائي طويل — **متغيّرهوش**، لو غيّرته كل الناس هتتطرد من حساباتها |
| `PUBLIC_ORIGIN` | `https://tawwerni.com` |
| `PAYMENT_INGEST_TOKEN` | نفس اللي في تطبيق الأندرويد |

للإيميلات (اختياري بس مهم — من غيرها مفيش رسايل ترحيب ولا لينكات تغيير باسورد):

| المتغيّر | القيمة |
|---|---|
| `SMTP_HOST` | `smtp.hostinger.com` |
| `SMTP_PORT` | `465` |
| `SMTP_USER` | `noreply@tawwerni.com` |
| `SMTP_PASS` | الباسورد |
| `EMAIL_FROM` | `noreply@tawwerni.com` |

`PUBLIC_ORIGIN` مهم بالذات: لينك "نسيت كلمة السر" بيتبني منه. لو ناقص،
اللينك هيتبعت على الدومين الافتراضي وممكن ما يشتغلش.

---

## بعد الرفع — الفحص

| # | افتح | المفروض تشوف |
|---|---|---|
| ١ | `tawwerni.com` | الصفحة **بتصميم كامل**، مش نص أسود على أبيض |
| ٢ | `tawwerni.com/login` | فورم دخول — مش شاشة سودا |
| ٣ | `/forgot-password` | صفحة طلب لينك تغيير الباسورد |
| ٤ | `/admin` | فورم إيميل + باسورد، وبعد الدخول لوحة الطلبات |
| ٥ | `/admin` فوق يمين | **حالة الإيميل** — لو "مش متظبط" حط متغيّرات SMTP |
| ٦ | `/app` | الهيدر ثابت فوق، و**٦ مسارات** في الشبكة |

**لو الصفحة طلعت من غير تصميم:** الـ build ما خلصش أو `.next` ناقص —
شغّل `rm -rf .next && npm run build` تاني واعمل Restart.

---

## لو حصلت كارثة

عندك `dev.db.backup` في مجلد الهوم:

```bash
cp ~/dev.db.backup dev.db
```

بعده Restart. البيانات رجعت.
